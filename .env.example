# ─────────────────────────────────────────────────────────────
# AI 프로바이더 (진단 전체를 AI로 구동)
# ─────────────────────────────────────────────────────────────

# 공개(배포) 버전 — Claude 사용. 이 키가 있으면 공개 버전이 AI로 동작합니다.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# 내부 테스트 전용 — Grok 4.5. 아래 AI_MODE=internal 일 때만 동작합니다.
# (공개 트래픽에서는 절대 사용되지 않습니다.)
AI_MODE=
XAI_API_KEY=
XAI_MODEL=grok-4.5

# 선택: 키워드 3층 전략도 AI로 생성
AI_KEYWORD_STRATEGY=false

# 선택: localhost 진단 라우트 보호
DIAGNOSE_API_KEY=
